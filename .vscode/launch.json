{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Piano Precision",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/Piano Precision",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "build-piano-precision"
    }
  ]
}